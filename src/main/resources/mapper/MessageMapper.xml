<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zxy.dao.MessageDao">
    <insert id="saveOrUpdateUserData">
        <!--<selectKey keyProperty="count" resultType="int" order="BEFORE">-->
            <!--select count(*) from userdata where id = #{id};-->
        <!--</selectKey>-->
        <!--<if test="count > 0">-->
            <!--update userdata set time=#{time},username=#{username},name=#{name},size=#{size},num=#{num},weight=#{weight},priceWithTax=#{priceWithTax},price=#{price},salesman=#{salesman},invoice=#{invoice}-->
        <!--</if>-->
        <!--<if test="count == 0">-->
            insert into userdata (time,username,name,size,num,weight,priceWithTax,price,salesman,invoice) values (#{time},#{username},#{name},#{size},#{num},#{weight},#{priceWithTax},#{price},#{salesman},#{invoice})
        <!--</if>-->
    </insert>
</mapper>